#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "This is just a template if I'd be doing tests. This actually builds if you add the test folder in the CMake project." << std::endl;
    return 0;
}